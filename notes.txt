-- todo--

- render the display

   pixel on '█'
   pixel off '░'


pending things:

use windows::Win32::{
    Foundation::HANDLE,
    Security::SECURITY_ATTRIBUTES,
    Storage::FileSystem::{
        CreateFileA, FILE_ACCESS_FLAGS, FILE_FLAGS_AND_ATTRIBUTES, FILE_GENERIC_READ,
        FILE_GENERIC_WRITE, FILE_SHARE_WRITE, OPEN_EXISTING,
    },
};

    //trying to get handle to console buffer
    unsafe {
        let console_file_handle = CreateFileA(
            "CONOUT$",
            FILE_GENERIC_READ | FILE_GENERIC_WRITE,
            FILE_SHARE_WRITE,
            &SECURITY_ATTRIBUTES::default(),
            OPEN_EXISTING,
            FILE_FLAGS_AND_ATTRIBUTES::default(),
            HANDLE::default(),
        )
        .unwrap();
    }